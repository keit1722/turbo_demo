<div class="container mx-auto px-4 py-4">
  <% flash.each do |flash_type, message| %>
    <div class="<%= flash_class(flash_type) %> mb-4 p-4 rounded shadow">
      <%= message %>
    </div>
  <% end %>
</div>
